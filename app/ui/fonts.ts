import { Inter, Lusitana } from "next/font/google";

export const inter = Inter({ weight: "variable", variable: "--font-inter", subsets: ["latin"] });
export const lusitana = Lusitana({ weight: ["400", "700"], subsets: ["latin"] });
